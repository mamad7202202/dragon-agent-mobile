// Placeholder replaced by real smoke tests — kept so `flutter create .`
// in CI never injects a default counter test referencing MyApp.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
